#include <bits/stdc++.h>

using namespace std;

int main() {

    cout << ((1 | 6) ^ (3 | 5));
    // freopen("input.txt", "r", stdin);
    // int n, m;
    // cin >> n >> m;

    // int N = pow(2, n);
    
    // int a[N];
    // for (int i = 0; i < N; i++) {
    //     cin >> a[i];
    // }

    
    // for (int i = 0; i < m; i++) {
    //     int p, b;
    //     cin >> p >> b;


    // }


    return 0;
}