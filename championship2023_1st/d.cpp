#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;

    vector<int> c(n - 1);
    for (int i = 0; i < n - 1; i++) {
        cin >> c[i];
    }

    vector<int> d(n - 1);
    for (int i = 0; i < n - 1; i++) {
        cin >> d[i];
    }

    


    return 0;
}