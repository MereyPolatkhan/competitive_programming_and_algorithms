#include <bits/stdc++.h>

using namespace std;

int const N = (int)1e6 + 7;

vector<int> a(N);

int main() {

    // ios::sync_with_stdio(0);
    // cin.tie(0);
    // cout.tie(0);

    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }

    int m;
    cin >> m;

    for (int i = 1; i <= m; i++) {
        int l, r;
        cin >> l >> r;
        
    }


    return 0;
}