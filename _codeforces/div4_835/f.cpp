#include <bits/stdc++.h>

using namespace std;

void solve() {
    long long n, c, d;
    cin >> n >> c >> d;
    
    long long a[n];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}