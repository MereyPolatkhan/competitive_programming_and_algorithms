#include <bits/stdc++.h>

using namespace std;

void solve() {
    long long n;
    cin >> n;

    if (n % 2 == 1) {
        int m = n * 3;
        for (int i = 0; i < n - 1; i++) {
            
        }
    }
    else {

    }

}

int main() {
    // freopen("input.txt", "r", stdin);
    ios::sync_with_stdio(0);
    cin.tie(0);

    long long n;
    cin >> n;

    while (n--){
        solve();
    }

    return 0;
}