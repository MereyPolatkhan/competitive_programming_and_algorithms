#include <bits/stdc++.h>


using namespace std;

long long m, n;

struct triple {
    int t;
    int z;
    int y;
    triple() {
        t = z = y = 0;
    }
}

bool check(long long & x) { 
}

int main() {
    cin >> n >> m;

    vector<triple> a(n);

    for (int i = 0; i < n; i++) {
        cin >> a[i].t >> a[i].z >> a[i].y;
    }

    
    


    return 0;
}