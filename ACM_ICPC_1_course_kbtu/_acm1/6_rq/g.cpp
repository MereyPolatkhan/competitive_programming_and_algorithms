#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
const int N = 200000;


int main() {
    
    int t;
    cin >> t;
    while (t--) {
        char c;
        cin >> c;

        if (c == '?') {
            int i, j;
            cin >> i >> j;

        }   
        else if (c == '+') {
            int i, x;
            cin >> i >> x;
            
        }
    }




    return 0;
}