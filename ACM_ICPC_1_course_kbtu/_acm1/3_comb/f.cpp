#include <bits/stdc++.h>

using namespace std;

int n, k, p;

int combination(int n, int k) {
    
}

int main() {

    // freopen("comb.in", "r", stdin);
    // freopen("comb.out", "w", stdout);

    cin >> n >> k >> p;

    

    return 0;
}